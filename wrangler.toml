name = "reviewer-seo-tools"
compatibility_date = "2026-09-16"
pages_build_output_dir = "."

# This repository is a Cloudflare Pages project, not a Workers script.
# Use `wrangler pages deploy . --project-name reviewer-seo-tools` for CLI deployments.
